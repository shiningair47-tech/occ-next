import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  experimental: {},
};

// Note: Static page data collection for API routes is disabled during the build.
// Environment variables are injected at runtime on Vercel.

export default nextConfig;
